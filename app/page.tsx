import HomeClient from "./page.client";

export const metadata = {
  title: "Backed by Y Combinator",
  description: "Make your startup look like a YC startup",
};

export default function Page() {
  return <HomeClient />;
}
